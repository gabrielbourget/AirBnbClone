import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    scrollViewCradle: {
        height: 130, 
        marginTop: 20,
        paddingHorizontal: 5
    },
})

export default styles